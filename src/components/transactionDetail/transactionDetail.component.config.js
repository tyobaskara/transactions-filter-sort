import PropTypes from 'prop-types';

const displayName = 'TransactionDetail';
const propTypes = {
  transactions: PropTypes.object.isRequired
};
const defaultProps = {};

const Config = {
  displayName,
  propTypes,
  defaultProps
};

export default Config;
