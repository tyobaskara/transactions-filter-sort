import PropTypes from 'prop-types';

const displayName = 'StatusLabel';
const propTypes = {
  status: PropTypes.string.isRequired
};
const defaultProps = {};

const Config = {
  displayName,
  propTypes,
  defaultProps
};

export default Config;
